https://codeforces.com/problemset/problem/1928/D

Условие
Каждый тест состоит из нескольких наборов входных данных. Первая строка содержит одно целое число t (1≤t≤2⋅10^4) — 
количество наборов входных данных. 
Первая строка каждого набора входных данных содержит три целых числа n, b и x 
(1≤n≤2⋅10^5, 1≤b≤10^6,0≤x≤10^9)— количество рас и константы b и x
Вторая строка каждого набора входных данных содержит n
целых чисел c1,c2,…,cn (1≤ci≤2⋅10^5) — количества существ каждой из n рас.
Гарантируется, что сумма значений c1+c2+…+cn по всем наборам входных данных не превосходит 2⋅10^5.

Текстовый ход решения: прикинула, как смогла:(
Задаем переменную, которая равна количеству отрядов = a и a>=1
Тогда сила армии будет уменьшаться как (a-1)*x
Переменная c = количество существ в каждой расе и 1≤ci≤2⋅10^5
Тогда количество рас будет переменной n и 1<=n<=2⋅10^5
Чтобы вычислить количество b единиц к суммарной силе армии 
нужно найти отдельно общую сумму существ каждой расы во всех отрядах и поделить на 2, сложить полученные результаты каждой расы и 
умножить на b
в конкретном отряде (n1(а1)+n1(a2)...//2) (нам нужно целочисленное деление) + (n2(а1)+n2(a2)...//2)*b (что-то такого вида)
Нужна переменная, считающая увеличение силы армии = t и ее уменьшение = r
Нам нужен цикл, который будет комбинировать различные варианты t+r, и который сможет посчитать максимальную сумму t+r 
при заданных значениях переменных a,n,c

1.
import sys
input=sys.stdin.readline
F=lambda:[*map(int,input().split())]

def calc(m):
  ret=0
  for c in C:
    d=c%m
    n1=c//m #m-d개
    n2=n1+1 #d개
    ret+=n1*n1*(m-d)*(m-d-1)//2 + n2*n2*d*(d-1)//2 + n1*n2*(m-d)*d
  return ret*B-(m-1)*X

for _ in range(int(input())):
  N,B,X=F()
  C=F()
  l=1;r=max(C)
  ans=0
  while (l<=r):
    m1=(l*2+r)//3
    m2=(l+r*2)//3
    ret1=calc(m1)
    ret2=calc(m2)
    ans=max(ans,max(ret1,ret2))
    if ret1>ret2:r=m2-1
    else:l=m1+1
  print(ans)

- Импортируется модуль `sys`
- Задается функция `input`, которая считывает ввод с помощью `sys.stdin.readline`
- Задается лямбда-функция `F`, которая считывает ввод и возвращает список чисел
- Задается функция `calc`, которая принимает один аргумент `m`. 
  Внутри функции описан цикл, в котором для каждого элемента `c` из списка `C` вычисляется остаток от деления на `m`.
  Затем вычисляются значения `n1` и `n2`, которые являются частными от деления `c` на `m` и `n1+1` соответственно. 
  Далее происходят вычисления и добавление в переменную `ret`. В конце функция возвращает значение `ret*B-(m-1)*X`
- Запускается цикл `for` с количеством итераций, равным введенному числу. 
  Для каждой итерации считываются значения `N`, `B`, `X` и вызывается функция `F` для считывания списка значений `C`. 
  Затем задаются переменные `l` и `r`, которые задают начальные значения для диапазона поиска. 
  Затем задается переменная `ans` с начальным значением 0. 
  Запускается цикл `while`, который выполняется до тех пор, пока `l` меньше или равно `r`. 
  Внутри цикла вычисляются значения `m1` и `m2`, которые являются промежуточными значениями для поиска. 
  Затем вызывается функция `calc` для каждого значения `m1` и `m2` и вычисляются значения `ret1` и `ret2`. 
  Затем выбирается максимальное значение из `ans`, `ret1` и `ret2` и присваивается переменной `ans`. 
  Далее происходит проверка условия и изменение границ диапазона поиска. В конце цикла выводится значение `ans`.

2.
      import sys
    input = sys.stdin.readline
    t = int(input())
    for _ in range(t):
        n,b,x = map(int,input().split())
        a = list(map(int,input().split()))
        mx = max(a)
        cnt = [[] for _ in range(mx + 1)]
        S = [0] * (mx + 1)
        for i in range(n):
            for j in range(1,a[i]):
                cnt[j].append(i)
            S[a[i]] += a[i] * (a[i] - 1) // 2 * b
        ans = 0
        for i in range(1,mx + 1):
            S[i] += S[i - 1]
            c = 0
            for idx in cnt[i]:
                div, mo = divmod(a[idx], i)
                c += b * ((mo * (mo - 1) // 2 * (div + 1) * (div + 1)) + (i - mo) * (i - mo - 1) // 2 * div * div + mo * (
                            i - mo) * div * (div + 1))
            ans = max(c + S[i] - (i - 1) * x,ans)
        print(ans)

    Импортируется модуль sys.
    Записывается значение функции sys.stdin.readline в переменную input.
    Вводится целое число t с помощью функции input.
    Запускается цикл с количеством итераций, равным t.
    Вводятся три целых числа n, b, x с помощью функции map и метода split.
    Создается список a, в котором каждый элемент является целым числом, вводимым с помощью функции map и метода split.
    Присваивается переменной mx значение максимального элемента списка a.
    Создается список cnt, состоящий из пустых списков, количество которых равно mx + 1.
    Создается список S, состоящий из нулей, количество которых равно mx + 1.
    Запускается цикл с переменной i, принимающей значения от 0 до n-1.
    Внутри первого цикла запускается второй цикл с переменной j, принимающей значения от 1 до a[i].
    Для каждого значения j добавляется индекс и внешнего цикла в список cnt[j].
    К каждому элементу списка S добавляется значение a[i] * (a[i] - 1) // 2 * b.
    Присваивается переменной ans значение 0.
    Запускается цикл с переменной i, принимающей значения от 1 до mx.
    К каждому элементу списка S прибавляется значение предыдущего элемента.
    Создается переменная c, присваиваются значение 0.
    Запускается цикл по элементам списка cnt[i].
    Внутри цикла вычисляются значения div (целочисленное деление a[idx] на i) и mo (остаток от деления a[idx] на i).
    К переменной c прибавляется выражение, состоящее из нескольких частей.
    Печатается значение ans.

    Заметки:
    for – счетный цикл, повторяется определенное количество раз;
    while – условный цикл, повторяется до выполнения определенного условия.
    Ans - Часто, эта переменная используется, чтобы хранить результат последней выполненной операции или определенного действия. 
    Однако, по умолчанию Python не предоставляет встроенную переменную с именем `ans`, и ее нужно определить самостоятельно.
    Модуль sys обеспечивает доступ к некоторым переменным и функциям, взаимодействующим с интерпретатором python. 
    sys. argv - список аргументов командной строки, передаваемых сценарию Python.
